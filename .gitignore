# Never commit credential exports — these contain live passwords and TOTP secrets.
*.csv
*.json

# Your real split rules contain personal data (emails, phones, sometimes
# passwords used as logins). Keep them local. The example is safe to commit.
split_rules.json
!split_rules.example.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
